---
normativeVersion: "0.1"
normativeStatus: normative
title: Rust SDK
description: 安装并导航 MissionWeaveProtocol 0.1 的完整固定 Rust 运行时参考。
---

import { CardGrid, LinkCard } from "@astrojs/starlight/components";
import SdkInstall from "../../../../../../../components/SdkInstall.astro";
import SdkRuntimeMatrix from "../../../../../../../components/SdkRuntimeMatrix.astro";

# Rust SDK

`missionweaveprotocol` 是官方 Rust 箱子。此参考描述了确切的提交
`2727f8777737265d98dde4ceaca306612ef54c52`、crate版本 `0.1.0`、Rust
1.85 或更高版本以及版本 2024。记录的 pin 未在 crates.io 上发布；不同的 Git 修订版或包存档是不同的实现输入。

:::note[Implementation note]

<SdkInstall
  sdk="rust"
  command={`git clone https://github.com/MissionWeaveProtocol/rust-sdk.git missionweaveprotocol-rust-sdk
cd missionweaveprotocol-rust-sdk
git checkout --detach 2727f8777737265d98dde4ceaca306612ef54c52
cargo test --locked --all-features
cargo package --locked`}
  verifyCommand={`cargo run --locked --quiet --bin missionweaveprotocol-conformance
cargo install --locked --path . --bin missionweaveprotocol-conformance
missionweaveprotocol-conformance --verbose`}
>
  <p slot="notes">
    应用程序将库与 Cargo Git 依赖项绑定，其 <code>rev</code>{" "}
    是上面的确切提交。仅当其出处解析为相同字节并且其嵌入式协议包通过记录的验证调用时，供应或打包的副本才是可接受的。
  </p>
</SdkInstall>

:::

`missionweaveprotocol-conformance` 二进制文件接受可选的
`--verbose`。它运行嵌入的结构模式向量；它不建立 Registry 货币、Admission
Log 持久性、网络安全性或生产准备情况。

## 使用此参考

<CardGrid>
  <LinkCard
    title="完整运行时"
    description="每个crate 根重新导出、编解码器、捆绑验证器、CLI 边界和显式非实现。"
    href="./runtime/"
  />
  <LinkCard
    title="首次准入与历史信任"
    description="Registry 当前证据、可信上下文和 Admission Log 的类型化 trait，以及一个可运行示例。"
    href="./admission/"
  />
  <LinkCard
    title="确切的公共 API"
    description="每个公共crate 根声明都解析到其确切的源代码行。"
    href="./api/"
  />
</CardGrid>

## 支持记录的提交

<SdkRuntimeMatrix sdk="rust" />

该包提供进程内协议行为，而不是部署权限。应用程序仍然提供经过身份验证的当前和历史 Registry 证据、可信准入上下文以及权威的仅追加 Admission
Log。

---
normativeVersion: "0.1"
normativeStatus: normative
title: Go First Admission et confiance historique
description: >-
  Adaptateurs Go typés, ordre d'appel exact, limites de confiance, exigences de
  journal atomique et exemple d'admission exécutable.
---

import goAdmissionExample from "../../../../../../../../examples/sdk/go/admission/main.go?raw";

# Go First Admission et confiance historique

Le Go `AdmissionService` superpose la première admission et la confiance
historique au-dessus du vérificateur Signed Document en six étapes
([MWP-ADM-005](../../../../reference/specification/first-admission-and-historical-trust/#mwp-adm-005)).
Le flux indépendant de la langue est la
[page d'exécution locale de première admission et de confiance historique](../../../runtime/first-admission-and-historical-trust/),
et les exigences exactes sont les
[clauses locales de première admission et de confiance historique](../../../../reference/specification/first-admission-and-historical-trust/).

## Adaptateurs et résultats publics

| Public API                                   | Responsabilité                                                                                                                                                                                                                                                                                                  |
| -------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `AdmissionCurrentKeyResolver.ResolveCurrent` | Renvoie la preuve complète Organization Registry selon laquelle le déploiement affirme qu'elle est à jour pour cette décision.                                                                                                                                                                                  |
| `TrustedAdmissionContext.Issue`              | Émettez l'ID d'enregistrement de confiance, l'acceptation instantanée et l'acceptation du service uniquement après une absence faisant autorité.                                                                                                                                                                |
| `AdmissionLog.Lookup`                        | Renvoie un enregistrement authentifié trouvé ou `AdmissionLookup{AuthoritativeAbsence: true}`. Échec de l’échec du cache, du délai d’attente, de l’absence non authentifiée et de l’état indéterminé sous [MWP-ADM-003](../../../../reference/specification/first-admission-and-historical-trust/#mwp-adm-003). |
| `AdmissionLog.AppendOrReturnExisting`        | Ajoutez atomiquement des octets candidats ou renvoyez le gagnant faisant autorité simultané via une identité de service authentifiée.                                                                                                                                                                           |
| `AuthenticatedAdmissionRecord`               | Liez `RecordBytes` au service authentifié par l'adaptateur afin que [MWP-ADM-009](../../../../reference/specification/first-admission-and-historical-trust/#mwp-adm-009) puisse valider l'enregistrement renvoyé.                                                                                               |
| `AdmissionService.PrepareFirstAdmission`     | Créez et validez des preuves de candidat à partir d'un SDK produit par `VerifiedSignedDocument`. Cela n’ajoute ni n’implique une admission.                                                                                                                                                                     |
| `AdmissionService.AdmitFirst`                | Effectuez la vérification actuelle, la recherche faisant autorité, la création de candidats, l'ajout ou le retour atomique de l'existant et la validation des enregistrements renvoyés sous [MWP-ADM-006](../../../../reference/specification/first-admission-and-historical-trust/#mwp-adm-006).               |
| `AdmissionService.VerifyHistoricalAdmission` | Réexécutez la vérification en six étapes avec les preuves historiques Registry et exigez un enregistrement existant sans émettre de contexte ni ajouter d'ajout, comme l'exige [MWP-ADM-008](../../../../reference/specification/first-admission-and-historical-trust/#mwp-adm-008).                            |
| `AdmittedSignedDocument`                     | Renvoyez les preuves d'enregistrement vérifiées et analysées ainsi qu'une copie défensive `RecordBytes()`.                                                                                                                                                                                                      |

Un booléen de confiance fourni par l'appelant n'est pas un résultat de recherche
d'admission et ne doit jamais sélectionner le chemin de réussite.

## Ordre d'appel de première admission

:::note[Informative example]

```go
first, err := missionweaveprotocol.NewAdmissionService().AdmitFirst(
    missionweaveprotocol.SignedDocumentCommand,
    documentBytes,
    currentRegistry,
    admissionLog,
    trustedContext,
)
```

:::

`AdmitFirst` termine les six étapes du document signé avant de consulter le
journal. Un enregistrement trouvé est validé et renvoyé sans émettre de contexte
fiable ni ajout. Après une absence faisant autorité, le service émet un contexte
approuvé, appelle `PrepareFirstAdmission`, appelle `AppendOrReturnExisting`,
capture la valeur renvoyée et valide l'enregistrement réellement renvoyé. Un
gagnant simultané n'est acceptable que lorsque son schéma, son service
authentifié, sa liaison de document et son délai de confiance sont passés
[MWP-ADM-009](../../../../reference/specification/first-admission-and-historical-trust/#mwp-adm-009)
et
[MWP-ADM-010](../../../../reference/specification/first-admission-and-historical-trust/#mwp-adm-010).

## Relecture historique

:::note[Informative example]

```go
historical, err := missionweaveprotocol.NewAdmissionService().VerifyHistoricalAdmission(
    missionweaveprotocol.SignedDocumentCommand,
    documentBytes,
    historicalRegistry,
    admissionLog,
)
```

:::

La relecture historique réexécute la vérification avec l'historique Registry
conservé, nécessite un enregistrement trouvé et n'appelle jamais
`TrustedAdmissionContext.Issue` ou `AdmissionLog.AppendOrReturnExisting`. Les ID
d'enregistrement correspondant démontrent la récupération du même enregistrement
faisant autorité ; ils ne prouvent pas la fraîcheur Command, l'autorisation du
signataire, l'acceptation de la machine d'état ou la vérification portable de la
preuve de journal. Ceux-ci restent distincts sous
[MWP-ADM-013](../../../../reference/specification/first-admission-and-historical-trust/#mwp-adm-013)
et
[MWP-ADM-014](../../../../reference/specification/first-admission-and-historical-trust/#mwp-adm-014).

## Surface d'erreur

`AdmissionError.WireCode()` renvoie `AUTH_INVALID_SIGNATURE`, tandis que
`ProtectedDiagnostic().Stage()` renvoie `admission`. Les raisons protégées
stables distinguent les preuves manquantes, contradictoires, mal formées, non
authentifiées, indisponibles, indéterminées et auto-ancrées tout en préservant
le résultat de fil non oraculaire requis par
[MWP-ADM-012](../../../../reference/specification/first-admission-and-historical-trust/#mwp-adm-012).

Les adaptateurs de déploiement doivent renvoyer `NewAdmissionAdapterError` avec
une raison prise en charge lorsqu'ils ont besoin du service pour remapper une
panne locale approuvée. Les erreurs arbitraires ne constituent pas une preuve
d’absence d’autorité.

## Exemple d'exécution exacte épinglée

:::note[Informative example]

L’exemple dérive de manière déterministe une clé Ed25519, construit un Registry
complet et signé Command, et fournit des adaptateurs en mémoire saisis. Un
`sync.Mutex` protège la carte faisant autorité partagée. `Lookup` et
`AppendOrReturnExisting` l'acquièrent indépendamment ; le mutex rend
`AppendOrReturnExisting` atomique lorsqu'il recherche un gagnant simultané et
l'ajoute sous condition. Le protocole nécessite une recherche faisant autorité
et un comportement d'ajout ou de retour atomique existant, et non une
transaction couvrant les deux appels. La graine fixée est du matériel de
démonstration et ne doit jamais être utilisée en dehors de l’exemple.

<pre>
  <code>{goAdmissionExample}</code>
</pre>

Exécutez-le à partir d'une extraction exacte SDK :

```bash
go test ./...
go run /absolute/path/to/examples/sdk/go/admission/main.go
```

La sortie attendue contient le même ID d’enregistrement pour `first admission`
et `historical replay`. Le programme affirme également des hachages de signature
et des octets d'enregistrement correspondants, ainsi qu'un seul ajout.

:::

:::note[Implementation note]

Remplacez `currentRegistryFixture` par des adaptateurs Registry actuels et
historiques authentifiés, `inMemoryAdmissionLog` par un journal durable
authentifié qui renvoie des résultats de recherche faisant autorité et rend
`AppendOrReturnExisting` atomique sur la clé logique, et
`fixedTrustedAdmissionContext` par Heure de confiance contrôlée par Organization
et émission de service d'acceptation. Un déploiement peut utiliser une
transaction plus large, mais le protocole ne nécessite pas qu'une seule
transaction s'étende sur `Lookup` et `AppendOrReturnExisting`.

Remplacez `deterministicSigningKey` par un service de clés appartenant au
déploiement. Conservez les copies d’octets défensives et ne réduisez pas les
limites d’autorité en un seul booléen de confiance fourni par l’appelant.

:::
